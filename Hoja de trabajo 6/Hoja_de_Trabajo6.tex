\documentclass[8pt, a4paper]{article} %en clase va 8pt, 11pt, 12pt, a4paper, letterpaper
  \usepackage[left=12mm, top=0.5in, bottom=5in]{geometry}  
  \usepackage[utf8]{inputenc}%tildes
  \author{Juan Diego Vega Ruiz} %Autor
  \date{25 de Marzo de 2018} %Fecha
  
\begin{document} %Comenzar el doc
  \title{Hoja de trabajo 6} % nombre del titulo
   \maketitle % reafirmar titulo
    
    \section{Ejercicio 2} %seccion (es decir, subtitulo)      
    \subsection{Problema 1} % subseccion (es decir, subtitulo de la seccion)
      $n+0=n$:\\
            $\sigma(0) + \sigma(n)$\\
            $\sigma (0 + n)$\\
            $\sigma(n)$
    \subsection{Problema 2}
      $n+m = m+n$:\\
            $\sigma(\sigma((0)) + \sigma(\sigma(\sigma((0)))$ o \\
            $\sigma(\sigma(\sigma((0))) + \sigma(\sigma((0))$ =\\
            $\sigma (\sigma(\sigma(0)) + \sigma(0))$\\
            $\sigma(\sigma(\sigma(\sigma(\sigma(0))))))$
    \subsection{Problema 3}
       $n \otimes \sigma(\sigma(0)) = n \otimes n$:\\
            $\sigma(n) \otimes \sigma(\sigma(0))$\\
            $\sigma (\sigma(0) \otimes n)$\\
            $\sigma(n) \otimes \sigma(n)$
    
\end{document}